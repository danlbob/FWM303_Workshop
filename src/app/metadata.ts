import type { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'RetailStore',
  description: 'RetailStore application using AWS Amplify and Next.js',
};

export const viewport: Viewport = {
  themeColor: 'black',
};